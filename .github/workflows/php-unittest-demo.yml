name: Unittest
on: [ push ]
jobs:
  Run-Unit-Tests:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 Run tests. ${{ github.event_name }} event."
      - uses: php-actions/composer@v5
        with:
          php_version: 8
          version: 2