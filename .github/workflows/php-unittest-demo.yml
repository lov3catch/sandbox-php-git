name: Unittest
on: [ push ]
jobs:
  Run-Unit-Tests:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 Run tests. ${{ github.event_name }} event."
      - name: Install dependencies
        run: composer install
